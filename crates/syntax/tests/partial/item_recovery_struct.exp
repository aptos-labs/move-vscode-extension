module 0x1::item_recovery_struct {
    struct { }
        //^ expected identifier, got '{'
    struct <T> has
        //^ expected identifier, got '<'
                //^ expected ability
                //^ expected `;`, `{`, or `(`
    struct S has {}
              //^ expected ability
    struct S has copy,
                    //^ expected `;`, `{`, or `(`
    struct S has copy,, {}
                    //^ expected ability
    struct S { field: , field2: }
                   //^ expected type
                             //^ expected type
}
